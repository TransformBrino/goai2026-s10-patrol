# S10 踏步 / 楼梯 / 高台 训练计划 v2（09-13 晚，按作者现场经验重订）

依据：作者 09-13 现场经验；专家报告 A《官方遥控与动作拆解-0913晚》（scratchpad/agentD）、B《我方策略与官方差距根因-0913晚》（scratchpad/agentE）；官方录包 gftbzx1/gftb1/gfstj1（200 Hz）、flat_2208（楼梯 10 Hz）、flat_2246/2248（高台 50 Hz）、flat_2215（高台 10 Hz）。

## 0. 口径更正（先认错）
| 项 | 09-12 我们用的 | 复核后 | 后果 |
|---|---|---|---|
| 官方踏步三档速度 | 0.41 / 0.69 / 1.03 m/s | **≈1.0 / 1.4 / 1.9 m/s**（四轮均值把腾空轮的低转速算进去，压低 ≥1.7 倍；LIO 里程计独立印证） | 参考钟按错标签走：1.0 m/s 用了 1.49 Hz 的相位，我方步频被顶到官方 2～3 倍 |
| 官方踏步步频 | 1.11 → 2.01 Hz 随速度上升 | **0.5～1.9 m/s 恒 1.10 Hz、摆动恒 0.35 s，靠步幅提速** | 我方 H7：1.0/1.8/2.3 m/s 步频 2.4/3.2/3.9 Hz、摆动 0.11～0.15 s、步幅只有官方 40% —— 这就是作者看到的"踏得很快、不稳" |
| 官方上楼梯速度 | 0.2 m/s、1.75 s/级 | **0.50 m/s、1.0 s/级**（里程计与抬腿计数×踏面 0.55 两法一致，两晚录包一致） | 我方 E5 实际 0.53～0.60 m/s、0.9～1.06 s/级，**不比官方快**；但指令 0.2/0.4/0.5 实际都 0.53～0.60 → 旋钮无效，作者想慢也慢不下来 |
| 站高 | 训练默认位 0.42 | 官方：踏步中 0.345（膝 1.63）、静止 0.376（膝 1.28）、高台行驶 0.367（膝 1.48）、楼梯爬行四轮均 0.345（前轮 0.256/后轮 0.435，机身跟坡 −15°） | 我方踏步中 0.34～0.365（只高 0.5～2 cm）；**静止/低速 0.42、膝 0.62～0.71** —— 作者说的"高 10 cm、膝不弯"是静止/低速姿态，由 knee/hipy 回默认位罚 + stand_still 罚拉回 0.42 造成 |
| 官方高台动作 | 09-08 视频拆解：4.5 s 慢爬、蓄力下蹲、后腿贴面拖上 | **1 s 快动作**（右前起抬到四轮全上 0.76～1.02 s）；两后腿都折腿跨沿；左后蹬到近直是主力（膝 30～40 N·m）；起抬前不下蹲，只把后轮前收 0.1 m；六次一致，墙根几乎停住也能触发 | ClimbH 的训练目标要换模板 |
| 官方楼梯后腿 | 贴踢面滚（09-12）/ 抬 5.5～6.9 cm（19:4x） | **低抬 8～11 cm（≈踢面高）擦着踢面上**，前腿抬 15～20 cm、落在髋前 0.2 m | 后腿目标不是高抬，是"抬到踢面高 + 3 cm" |
| 官方遥控 | — | 楼梯：手柄钉在 0.42 不松手，只叠加小 yaw，松杆 1 s 内停；高台：满杆冲、起抬前 0.3～0.7 s 归零，从不后退蓄力 | 我方控制台"按 W 切专家、松开切回、起步 0.3"与之相容 |

## 1. 踏步主策略 T1（从 H7_11800 续，目标"官方的步频和姿态"）
目标：步频 1.10 Hz 恒定（≤1.9 m/s）、摆动 0.35 s、踏步站高 0.345、静止站高 0.376/膝 1.28、坡上不乱。
1. 参考：重贴 ref_step 的速度标签（1.0/1.4/1.9 m/s，新增 0.5 m/s 档，频率一律 1.10 Hz，2.0 m/s 以上 1.5 Hz）；参考钟频率随指令按官方表插值。
2. 步态项：`sg_idle` t_max 0.6 → 1.0 s（去掉 1.67 Hz 的步频下限）；`sg_swing` 目标 T(v)=0.35 s 恒定，权重 8 → 3；`sg_hold` 0.5 → 0.6，`sg_clear` max_dur 0.4 → 0.45。
3. 姿态：`knee/hipy_joint_pos_penalty` 减半、stand_still_scale 5 → 1；`base_height_flat` 目标 0.345（有指令）/ 静止段目标 0.376；不动 runner 的 q_default（待作者拍板 Q3）。
4. 坡：`step_gait._swing` 基准从"四轮世界最低点"改为"该轮自己最近触地高"（同 front_lift_each 口径），坡 >5° 不再全员误判；主策略不需要爬楼 → `wheel_height_progress/record` 与 st_* 上墙八段权重全部 0（坡上冲刺的来源）；`split/wall_ahead` 不再触发上墙相位；地形：坡 12% → 30%（5～20°）、粗糙 25%、平地 35%、箱 10%。
5. 指令：vx (−0.8, 2.0)（待拍板 Q2）、vy ±0.5、wz ±1.5，关航向外环（同 H7）。
6. 训练：600 步 LR 1e-4、std 0.3；探针 300/600（机体系）：步频、摆动时长、站高、坡 10°/15° 上 0.5/1.0 指令不冲不停踏。
验收：平地 0.5/1.0/1.8 步频 1.0～1.3 Hz、摆动 ≥0.3 s、站高 0.33～0.36；静止站高 0.37～0.39；10°/15° 坡 0.5/1.0 指令实际 ±0.15、四轮都踏；摩擦 0.3 不摔；切爬台/切专家 2/2；倒退 −0.5 能退不打转。

## 2. 楼梯专家 S1（从 E8_12398 续，目标"旋钮管用 + 平地全向遥控 + 后腿低抬"）
1. 速度旋钮：`wheel_height_progress/record` 300/200 → 100/50 且按"|v−指令|<0.15"门控；参考钟频率 ∝ 指令（0.5 m/s ↔ 1.10 Hz，0.25 ↔ 0.55 Hz）；超速罚 −1 → −10、margin 0.1 → 0.05；速度跟踪 σ 0.707 → 0.3；指令 vx (0.1, 0.5)（平地段允许 −0.3）；max_rise 1.0 → 3.0（决赛 4.75 m 落差不饱和）。默认档待拍板 Q1。
2. 平地全向：`ss_riser_push` 只在翻越相位内算（平地打滑/横推不罚）；去掉 M20 三项转向步态奖励对 gait_level 课程的依赖（固定 1）；hipx 罚在 |vy|>0.1 或 |wz|>0.1 时豁免；转速跟踪 σ → 0.3；平地地形 30%；训练 1200 步（E8 只训了 600 步，横移才开始有梯度）。
3. 后腿低抬：抬高奖励改成从 0 起有梯度的线性/宽核，目标 = 踢面高 + 3 cm（0.10～0.14），按"后轮过沿净空"直接奖；`phase_only` 改为按轮门控；`WheelHeightProgress` 的势函数用"最后触地高"（抬腿瞬间不再被扣 −50）；参考轨迹加入官方后腿摆动模板（hipy +0.7→0，knee −1.7→−1.0，0.2～0.45 s）；保留 ≥3 轮着地。
4. 训练：1200 步 LR 1e-4；每 200 步扫描。
验收：全程专家 26 条 + 楼梯听指令验收（平地：站 0 速 8 s 漂 <0.1 m/偏航 <5°，前进 0.4/倒退 0.3/横移 ±0.3/原地转 ±0.5 各达指令 70% 以上）+ 速度旋钮（指令 0.25/0.5 → 每级 2.0±0.3 / 1.0±0.2 s）+ 后轮越沿净空 ≥3 cm、顶踢面 <5%。

## 3. 高台专家 C1（从 ClimbH_8797 续，目标"官方 1 s 折腿翻越模板 + 平地全向遥控"）
1. 参考模板：2246/2248 六次翻越关键帧（相对右前起抬 t₀）做成 1.0 s 动作参考：t₀−0.3 后腿前收；t₀ 右前 knee 1.3→2.7；t₀+0.3～0.5 机身抬头 −38°（不保持）；左后蹬直；右后折腿跨沿；左后被拉高再折腿跨沿；落平。奖励：相位跟踪 + 两后腿各自"过沿净空 ≥3 cm"直接奖 + ≥3 轮受力。
2. 删掉与官方不符的旧目标：45° 保持、蓄力下蹲、后腿贴面拖上（st_hind_extend/st_level_drag 等归零）。
3. 平地全向遥控与楼梯专家同一套改法（§2.2）；接近速度指令 (0.1, 1.0)，墙根停住也要能触发（官方两次近停触发成功）。
4. 训练：1500 步；验收：bench_top33 ≥7/8 且后轮不贴面拖、侧倾峰 <15°、翻越用时 1～2 s；平地全向验收同 §2。

## 4. 顺序与时间（GPU 单卡，串行；待拍板 Q4）
- 今晚 S1（约 1.5 h 训练 + 扫描）→ T1（通宵，约 1 h）→ 明早 C1。
- 每条线出包都附：说明.md（口径、指令范围、已知缺陷）、验收结果、检查点扫描表。均需真机验收后才算过。

## 5. 请作者拍板（四条）
Q1 楼梯默认档：0.3 m/s（每级约 2 s，比官方慢一倍）还是 0.5（官方节拍）？建议 0.3 起、旋钮 0.1～0.5。
Q2 踏步最高速：2.0（官方实测最高 1.9～2.2）还是坚持 2.5？建议 2.0，步频钉 1.10 Hz。
Q3 静止站姿：只改奖励目标（不动 runner）还是同时把 runner 默认位改成官方蹲姿（hipy ∓0.72、knee ±1.44，需 AGX 改 runner）？建议先只改奖励目标。
Q4 顺序：S1 → T1 → C1？

## 6. 补遗（09-14 00:1x 作者问"有没有遗漏"后补上的四项）
1. **楼梯专家的动作质量目标**（作者"上台阶不稳定"的另一半，原 §2 只写了速度）：前腿摆动 0.2～0.25 s（我方 0.81）、相对机身抬高 0.15～0.20 m、落点在髋前 0.18～0.21 m；摆动中轮子前转 ≤9 rad/s（我方空转 16～19，是"甩腿"观感的来源）；后腿摆动 0.2～0.45 s、抬到踢面高 +3 cm；机身跟坡 pitch −15±5°、roll p95 ≤6°（我方 10～18°）；爬楼中四轮均站高 0.345（前轮 0.256/后轮 0.435）。做法：参考轨迹换成官方前/后腿摆动模板（报告 A §5.1）、摆动时长目标 0.25 s、空中轮速罚（>9 rad/s 的部分）、roll 罚加倍。验收加：摆动时长、空中轮速、roll p95、站高四项。
2. **越野/坡验收台架**：T1 验收加 10°/15° 斜坡（agentE/mjcf_copy/mjcf/slope10|15.xml，今晚已复现问题）和随机粗糙地（新建 bench_rough：±3 cm 随机高度场 + 草皮摩擦 0.6），指令 0.5/1.0 各 2 条：实际速度 ±0.15、四轮都踏、roll p95 ≤8°、不冲不停。训练地形加 real_patch/粗糙 25%。
3. **感知鲁棒性**（21:24 侧翻的另一半原因）：训练观测噪声 corrupt_height 加"整幅偏置 ±0.10 m"和"前方 0.4～0.8 m 整幅深值（−0.3）"两种假象（各 5% 概率，E/T/C 三条线都加），让策略见过 LIO z 漂造成的假图不至于抬双前腿；AGX 侧的 LIO z 自检（站立 base 高≈0.42，偏 >5 cm 提示/重启）按 AGX 明日计划做。
4. **录包覆盖与分辨率**：七段官方录包（gftbzx1/gftb1/gfstj1 200 Hz；flat_2246/2248 50 Hz；flat_2208/2215 10 Hz）全部拆过；节拍/速度/站高/遥控方式用 200/50 Hz 段定，10 Hz 段只做印证（互差 ≤5%）。没有的数据：真机上我方踏步中的站高（下次跑 H7/T1 开 S10_OBS_DUMP）、决赛楼梯的踢面/踏面实测值（按 0.11～0.15 / 0.55 假设，作者有卷尺数请给）、高台"左前先上"的样本（六次全是右前先上，训练两侧都练）。

## 7. 09-14 真机复盘补记（16:2x 两段录制）
- F 在真实楼梯 13 级后卡住/翻车：后轮不抬 + 楼梯比仿真验收陡窄（里程计约 31° vs 验收场景 9.5°）。仿真新场景 0.13×0.30 / 0.15×0.30 复现（F、A 都翻）。**验收场景改为陡窄楼梯**，S12 起训练难度 0.6～1.0、窄踏面为主、后轮抬高 +10。
- T3 主策略真机退化为"举一只轮三轮滚"（MuJoCo 后续检查点同样）：踏步振荡缺鲁棒性，T6 加执行器延迟/PD/接触随机化；期间主策略用 V1H_9196。
- 高台 ClimbH 在双 Airy 图下正常；C2 温和版在训。

## 8. 09-14 晚 Isaac↔MuJoCo 对拍与两条方法论（20:2x）

### 8.1 对拍结论（踏步线）
用 `physx_climb_eval.py --terrain flat --dump_obs`（Isaac 侧，新增 flat 地形）与 runner 的 `S10_OBS_DUMP`
（MuJoCo 侧）在平地 0.5 m/s 下逐列比 244 维观测：
- **均值完全一致**——接口、量纲、关节顺序、指令缩放、高程扫描基准全部对齐，这一整类假设可以排除。
- 训练里开着的恢复系数（机器人 0~0.7 × 地形 1.0，MuJoCo 为 0）单独做了对照实验：把它设成 0 后
  Isaac 侧波动几乎不变（角速度 σ 0.0794 → 0.0770），**不是根因**。
- 差别在波动：T6_13999 在 MuJoCo 里**四条腿完全锁死、纯轮子滚**（膝关节位置 σ 0.0021~0.0037、
  膝关节速度 σ 0.0002~0.0005、动作 σ 0.003）；T3_13400 同场景 0.033~0.069 / 0.019~0.034 / 0.24~0.44。
  接触统计报的"右前悬空 91%"不是举腿，是姿势歪了那只轮碰不到地。

### 8.2 新判据：平地 0.5 m/s 膝关节位置 σ（`scratchpad/knee_sd.py`，已接入 probeT1）
不依赖接触判定，Isaac 与 MuJoCo 同口径。门槛：MuJoCo 侧 **最小腿 σ ≥ 0.03 = 真在踏步**；
0.015~0.03 偏弱；< 0.015 判"腿锁死/纯滚 ⛔"。两边衰减比约 0.5，故 Isaac 侧要 ≥ 0.06。

### 8.3 方法论两条（这轮的教训，后续每次加奖励项都按此走）
1. **接触类反作弊项治不了"腿不动"**。`sg_air_frac`（悬空占比）、`air_frac_lr_penalty`（左右不对称）
   在退化终态下读数都是 0——没有摆动就没有悬空占比可言。反作弊项必须量**策略能直接控制、且退化态下
   仍非零**的量（关节摆幅、关节速度），不能量接触派生量。
2. **新奖励项开训前必须先验读数与区分度**：拿一个已知坏点和一个已知好点各跑 20~25 迭代，确认
   ①读数非零 ②坏点明显高于好点 ③量级压得过被绕过那一项的收益。T6 白跑 600 步就是漏了这一步；
   T7 的 `knee_amp_penalty` 按此验过：坏点 6.27 / 好点 2.27（含 sg_idle），差 4.0 > 作弊收益 1.0。

### 8.4 高台线（C4/C5）
- C3 后轮"贴立面拖上来"的奖励源找到了：`wheel_height_progress`（w=100）对顶着立面往上蹭一路给分
  （源码注释原话"轮子沿立面贴着滚上去时一路有分"，早期起步奖励）。C4 起 `PROGRESS_SKIP_PRESSED`
  只算"触地且没顶立面"的轮。
- 度量换成**越沿净空**（后轮腾空时高出记住的台沿高度，封顶 8 cm），与官方"两条后腿折叠跨过沿"
  直接对应；原来的"相对自己起抬点抬高"在蹭立面时起抬点一路被刷新，读不出区别（实测 0.037）。
- C4 训练里该项 +0.029 → +0.272（9 倍）、后轮顶立面罚 −0.290 → −0.113。
- **C4 暴露的洞**：高台专家 `rel_standing_envs = 0.0`、`lin_vel_x (0.3, 0.6)`——从没见过零指令，
  所以学不会平地停住（实测漂 2.0~8.6 m），加的零指令动罚一个样本都罚不到。C5 给 10% 零指令样本。
- 侧倾暂不加码：C4 全程 `cc_roll_ph` 持平，MuJoCo roll 峰 15~22° 与 C3 相当；官方翻高台本身是
  不对称动作，罚过头会压掉动作。**待作者从录制里给出官方翻台侧倾峰值**再定。

### 8.5 顺手修掉的脚本 bug（都会污染结论，记录备查）
- 扫描守卫 `grep -c` 多文件输出多行 → `[: 参数太多`，守卫一直没生效；
- 同一 ROS 域串行跑第 2/3 条时上一条 runner 没退干净被"拒绝开测"（S13 那轮 42 条里废了 9 条，
  其中 15800 那点 6/7 条是空跑，不算数）→ 等待放宽到 45 s + 重试 3 次；
- `sweepC.sh` 漏 `break`，扫完不退出；
- 扫描末点 off-by-one：rsl_rl 存 `model_(N-1)`、链传 `N`，末点既被当"非整百"跳过、退出条件也永不满足
  → 两个 sweep 都加 10 的容差；
- `body_probe2.py` 遇残缺 csv 崩溃、`g_verify_driver.py` 收尾汇总 PH 元组长度不符崩溃。

## 9. 09-14 夜 21:0x~21:2x：官方姿态口径修正 + 高台线的能力取舍

### 9.1 单位坑：真机录制的 roll/pitch 是**弧度**
`/IMU_DATA` 的 roll/pitch/yaw 按 SDK 定义是弧度
（`s10_ws/src/S10_sdk_deploy/interface/robot/robot_interface.h:83`：`roll-pitch-yaw(unit rad)`），
而 `scratchpad/bag2npz.py` 原来把原值直接落盘、列名却叫 roll/pitch。按度读差 57 倍，
曾读出"官方爬 2.7 m 楼梯只侧倾 0.2°"。已改成落盘前转度；旧文件的说明见
`~/s10_logs/night/official_gait/单位说明.md`。**09-13 那份《官方遥控与动作拆解》自己做过换算，结论不受影响。**

### 9.2 官方姿态口径（训练目标是**贴住它**，不是越小越好）
| 工况 | \|roll\| | pitch | 其它 |
|---|---|---|---|
| 翻高台 0.33 m（六次事件） | **≤15°（max 14.8）** | 峰 −38°（−30~−45），>30° 持续 ≤0.45 s | 落平过冲 ≤+12°；右前起抬→落平 1.0 s |
| 上楼梯（2246/2248/2208，坡 13.5~15°） | 峰 10.6~14.8°，p95 5.6~9.6° | 机身跟坡 −15.4°（p5/p95 −20.5/−9.7） | 22 级 × ≤0.134 m / 20.8 s |
| 平地踏步 | ≤2° | ±3° | |

对照我方：C4_9599 翻高台 roll 15~17°、pitch 36~37° —— **俯仰已在官方带内，侧倾只超 0~2°，基本达标**。
**楼梯线连带修正**：S13 设 `ss_roll = −25` 是按"越小越好"调的，属压过头（官方自己就有 10~15°），
策略宁可不动也不敢侧倾，这是它爬到第 11/12 级翻车的原因之一。楼梯线重开时按 ≤15° 设。

### 9.3 高台线的能力取舍（C5 实测给出的代价）
C5 给了全向指令（含倒退），从 C4_9599 起训 200 迭代：
- 拿到：倒退 −0.3 由 **+0.08（反着走）→ −0.29** ✓
- 丢掉：左后轮越沿净空 **0.095/0.086/0.131 → 0.014/0.000/0.000 m**（塌回 C3 水平）；
  侧倾 15~17° → 6~10°（退回左右对称的"贴着拖上去"）；转向仍未学会（wz +0.01）
- **登顶始终 3/3** —— 只看登顶的守卫抓不到这种退化。

作者 21:2x 拍板：**高台专家专做翻台阶，会前进 / 停止 / 拐弯即可，不要求后退。**
→ C6（从 C4_9599 起）：vx (0.2, 0.6) 去掉倒退、零指令 10%、wz ±0.5；
   跟踪权重 8/6 收回 6/5；越沿净空 30 → 80、后轮顶立面 −20 → −35。
→ 扫描守卫加第二条：**后轮越沿净空中位数 < 0.03 m 判退化、停训回退**
   （判据用历史数据验过：C4_9599 0.066 ✓ / C5_9600 0.086 ✓ / C5_9800 0.000 ⛔）。

### 9.4 踏步线定稿候选
`~/s10_logs/release/V1HTrotT7_13999`（md5 fe80caa4）：治好了 T3~T6 共同的"腿锁死纯滚"，
0.5 档四条腿 36/75/66/46（T3 的左后只有 23%）、零指令 0.06 m、转向贴指令；
代价是高速跟踪 1.8→1.44、2.3→1.49（T3 是 1.74/2.23）。T8（膝摆幅罚 −40→−12）正在排队找回高速。
