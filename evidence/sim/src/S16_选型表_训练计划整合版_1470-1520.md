多个函数共用同样的收尾语句是常态（本例 `edge_clearance` 与 `pitch_hold_reward` 结尾一模一样）。
安全做法：**按函数名定位行区间再改**，改完用 `ast` 核对"参数落在哪个函数"，并与备份 diff 未改动的函数。
冒烟测试今天第三次在浪费训练前拦住问题（前两次：T10 的 `mdp` 未导入、`root_quat_w` 是 ProxyArray）。

### 闭环 · S16 两个检查点（09-15 12:06/12:16）—— **门开了，但这一项太小、且与两件事冲突**

| | ① 爬楼段 pitch 均 | ② \|roll\| p95 | ③ 上楼 / 0.2 档 |
|---|---|---|---|
| S14_15000 基线 | −3.6° | 6.8° | 6/6 / 通过 |
| **S16_15200** | **−2.7°** | **6.7°** ✓ | **6/6 / 通过** |
| S16_15400 | −4.0° | **10.1°** ✗ | 6/6 / 通过 |

**门确实开了**（`ss_pitch_slope` S15 末期 0.038 → S16 开训 0.795，21 倍），
**但 pitch 没往 −10 走**（−2.7/−4.0 就在基线 −3.6 附近抖）。

**训练曲线说明策略根本没在优化它**：`ss_pitch_slope` 在 0.63~1.27 之间来回抖、**无上升趋势**，
而 `track_lin_vel_xy_exp` 是 **+11.0**，差 **10 倍**；爬楼时抬头必然牺牲前进速度，**两者直接冲突**，
策略理性地忽略它。
**另有一处耦合**：15400 那点 `ss_pitch_slope` 冲到最高 1.27 时，**侧倾恰好劣化到 10.1** ——
抬头与横向稳定也在打架。

**判断**：三轮（S15 目标错、S16 门修好）之后，跟坡仍没动，且已看清它同时与**速度跟踪**和**侧倾**冲突。
官方能同时做到 pitch −15.4 且 roll p95 6.8，说明物理上可行，但我方策略要换取。
**S16_15200 是个好点**（roll 6.7 优于基线、6/6、0.2 档过），**可作为楼梯线备选交付**。

**下一轮 S17（有界尝试，只做一轮）**：`ss_pitch_slope` 权重 **10 → 40**，起点 **S16_15200**。
现在加权重是**合法**的：门已开、核有梯度、读数随策略变化（0.63~1.27），
**剩下的唯一解释就是量级不够**（与 T12 那次"梯度方向没修好就加权重"不同）。
**硬边界**：若 ② roll p95 劣于 6.8 而 ① 才改善一点 → **确认这是真实取舍，停止投入，交付 S14_15000 或 S16_15200**。

### C23 冒烟"横幅没打"—— 链脚本的陈旧模式，不是配置问题
C23 实际**冒烟通过**（日志里 C20 父类 + C23 两条横幅齐全）。是 chain102 由 chain100 用 sed 派生时，
校验行里的 `"s10-climb-C21"` 没被替换（sed 规则是 `s/C22 /C23 /g`，带尾空格，而那里后面是引号）。
**chain100（C22）也带着同样的陈旧模式，却"通过"了 —— 因为 C22 继承 C21，父类横幅照样打。**
**一个靠继承侥幸成立的检查。** 已修 chain102 并加自检（任务名与横幅模式必须一致）。

**纪律 33**：**冒烟的"横幅检查"必须校验本类自己的横幅，且脚本派生后要自检模式与任务名一致。**
靠继承打出的父类横幅会让检查假阳性通过——C22 那轮就是这么过的，真正该发现的问题被掩盖了。

### 闭环 · S16 收官（09-15 12:2x）—— **跟坡与侧倾是真实取舍，建议停止投入**

| 检查点 | ① 爬楼段 pitch 均 | ② \|roll\| p95 | ③ 上楼 / 0.2 档 |
|---|---|---|---|
| S14_15000 基线 | −3.6° | 6.8° | 6/6 / 通过 |
| **S16_15200** | −2.7° | **6.7°** ✓ | **6/6 / 通过** |
| S16_15400 | −4.0° | 10.1° | 6/6 / 通过 |
| S16_15599 | **−5.1°** | **9.8°** | 6/6 / 通过 |

**三个点连起来是单调的取舍**：pitch −2.7 → −4.0 → −5.1（在改善），
侧倾 6.7 → 10.1 → 9.8（同步恶化）。**不是噪声。**

